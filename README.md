Here we are going to convert base64string to their respective file typ. for this u need file type , file url and file name,
we are window.atob() // convert base64 to binary and then return blob url (a file url)
